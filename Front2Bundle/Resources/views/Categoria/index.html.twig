{% set page_title = 'Categoria' %}
{% set page_subtitle = '' %}

{% extends '::base.html.twig' %}

{% block content %}
<div class="row">
    <div class="col-md-6">
        <!-- BEGIN SAMPLE TABLE PORTLET-->
        <div class="portlet box grey">
            <div class="portlet-title">
                <div class="caption"><i class="fa fa-money"></i>Categoria</div>
                <div class="tools">
                    <a href="javascript:;" class="collapse"></a>
                </div>
            </div>
            <div class="portlet-body">
                <button class="btn green btn-xs" onclick="window.location='{{ path('configurazione_categorie_new') }}';">Nuovo categoria</button></a>
                <div class="table-responsive">
                    <table class="table table-striped table-condensed table-hover">
                        <thead>
                            <tr>
                                <th>Categoria</th>
                                <th>Id</th>
                                <th width="40">Op.</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for entity in entities %}
                            <tr>
                                <td>{{ entity.categoria }}</td>
                                <td>{{ entity.id }}</td>
                                <td>                            <button class="btn yellow btn-xs" onclick="window.location='{{ path('configurazione_categorie_edit', { 'id': entity.id }) }}';">edit</button></a>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        <!-- END SAMPLE TABLE PORTLET-->
    </div>
</div>

{% endblock %}
