{% extends '::base.html.twig' %}

{% block content %}
<div class="page-header">
    <div class="icon">
        <span class="ico-cog"></span>
    </div>
    <h1>Scheda <small>Dettagli</small></h1>
</div>
<div class="row-fluid">
    <div class="span6">
        <div class="block">
            <table class="table">
                <tbody>                    <tr>
                        <th>Indirizzo</th>
                        <td>{{ entity.indirizzo }}</td>
                    </tr>                    <tr>
                        <th>Localita</th>
                        <td>{{ entity.localita }}</td>
                    </tr>                    <tr>
                        <th>Comune</th>
                        <td>{{ entity.comune }}</td>
                    </tr>                    <tr>
                        <th>Cap</th>
                        <td>{{ entity.cap }}</td>
                    </tr>                    <tr>
                        <th>Descrizione</th>
                        <td>{{ entity.descrizione }}</td>
                    </tr>                    <tr>
                        <th>Difformita</th>
                        <td>{{ entity.difformita }}</td>
                    </tr>                    <tr>
                        <th>Lat</th>
                        <td>{{ entity.lat }}</td>
                    </tr>                    <tr>
                        <th>Lon</th>
                        <td>{{ entity.lon }}</td>
                    </tr>                    <tr>
                        <th>Id</th>
                        <td>{{ entity.id }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

<button class="btn yellow btn-xs" onclick="window.location='{{ path('scheda') }}';">Torna indietro</button></a>
<a href="{{ path('scheda') }}">
    <span class="label label-inverse">Torna indietro</span>
</a>

            <button class="btn yellow btn-xs" onclick="window.location='{{ path('scheda_edit', { 'id': entity.id }) }}';">Modifica</button></a>{% endblock %}
