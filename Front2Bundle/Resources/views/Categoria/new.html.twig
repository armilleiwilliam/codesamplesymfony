{% set page_title = 'Categoria' %}
{% set page_subtitle = '' %}
{% extends '::base.html.twig' %}
{% form_theme form 'MetronicAdminBundle::form/empty.html.twig' %}

{% block content %}
<div class="row">
    <div class="col-md-6">
        <!-- BEGIN SAMPLE TABLE PORTLET-->
        <div class="portlet box red">
            <div class="portlet-title">
                <div class="caption"><i class="fa fa-money"></i>Nuovo Categoria</div>
                <div class="tools">
                    <a href="javascript:;" class="collapse"></a>
                </div>
            </div>
            <div class="portlet-body">
                {{ form_start(form) }}
                {{ form_rest(form) }}
                {{ form_end(form) }}
            </div>
        </div>
        <button class="btn yellow btn-xs" onclick="window.location='{{ path('configurazione_categorie') }}';">Torna indietro</button></a>
        <!-- END SAMPLE TABLE PORTLET-->
    </div>
</div>

{% endblock %}
